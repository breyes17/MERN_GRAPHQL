const MSG = {
  CLIENT_ALREADY_EXIST: 'CLIENT_ALREADY_EXIST',
};

const ERROR_TYPE = {
  CLIENT_ALREADY_EXIST: {
    message: 'Client already exist.',
    statusCode: 403,
  },
};

module.exports = {
  MSG,
  ERROR_TYPE,
};
